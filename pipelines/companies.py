"""Company URL discovery pipeline preview."""

from __future__ import annotations


async def fetch_company_urls_for_sector(sector_url: str) -> list[str]:
    """Discover company detail pages for one sector in the private implementation."""
    raise NotImplementedError("Private implementation omitted from public preview.")
