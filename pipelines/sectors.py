"""Sector discovery pipeline preview."""

from __future__ import annotations


async def fetch_tradable_sector_links(
    listing_url: str,
    ignored_sectors: list[str],
) -> list[dict[str, str]]:
    """Discover tradable equity-sector links in the private implementation."""
    raise NotImplementedError("Private implementation omitted from public preview.")
